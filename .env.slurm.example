# Configurações específicas para o worker SLURM containerizado
# Copie este arquivo para .env.slurm e ajuste os valores conforme sua infraestrutura

# Nome do worker SLURM
WORKER_NAME=slurm

# Concorrência do worker (quantos processos paralelos)
SLURM_WORKER_CONCURRENCY=2

# Nível de logging
LOGGING_LEVEL=INFO

# Configurações do cluster SLURM (se necessário sobrescrever)
# SLURM_CONF=/etc/slurm/slurm.conf
# SLURM_CLUSTER_NAME=your-cluster-name

# Diretório de logs específico para SLURM (se diferente do padrão)
# SLURM_LOG_DIR=/var/log/slurm

# Configurações adicionais do SLURM client
# SLURM_SUBMIT_DIR=/tmp
# SLURM_JOB_NAME_PREFIX=orchestration

# Configurações de autenticação MUNGE (se necessário)
# MUNGE_SOCKET=/var/run/munge/munge.socket.2